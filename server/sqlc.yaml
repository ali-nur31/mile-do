version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries/"
    schema: "./db/migrations/"
    gen:
      go:
        package: "repo"
        sql_package: "pgx/v5"
        out: "./internal/repository/db"
        emit_json_tags: true
        emit_interface: true